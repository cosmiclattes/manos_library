# Production Environment Configuration
# Copy this file to .env.prod and update with your production values
# NEVER commit .env.prod with real credentials to version control

# Database Configuration
# Use your production PostgreSQL database URL
# For cloud providers:
# - AWS RDS: postgresql://username:password@your-db-instance.region.rds.amazonaws.com:5432/library_db
# - Google Cloud SQL: postgresql://username:password@/database?host=/cloudsql/project:region:instance
# - Azure Database: postgresql://username:password@your-server.postgres.database.azure.com:5432/library_db
# - Heroku: Use DATABASE_URL from Heroku config
DATABASE_URL=postgresql://prod_user:secure_password@your-prod-host:5432/library_production

# Google OAuth Configuration
# Create separate OAuth credentials for production in Google Cloud Console
# Make sure to add your production domain to authorized redirect URIs
GOOGLE_CLIENT_ID=your-production-google-client-id
GOOGLE_CLIENT_SECRET=your-production-google-client-secret

# Application Configuration
# CRITICAL: Generate a strong secret key for production
# Use: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_TO_A_STRONG_SECRET_KEY_IN_PRODUCTION
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Application URLs
# Update these to your production domain
# Examples:
# - APP_URL=https://api.yourdomain.com
# - FRONTEND_URL=https://yourdomain.com
APP_URL=https://your-backend-domain.com
FRONTEND_URL=https://your-frontend-domain.com

# Optional: Environment identifier
ENVIRONMENT=production

# Optional: Enable/disable debug mode
DEBUG=False

# Optional: Allowed hosts/origins (comma-separated)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com
